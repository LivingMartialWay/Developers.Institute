#Mainline
#get all my items to buy
#remove all non veg
#save items in a Files
#########################################

#def get_all_the_items(filename):
    # get all the new_item_list
    #1. open file
    file = open(filename, "r")
    #2. chekc the file avail
    if file.mode == 'r':
        #3. read
        lines = file.read()
    else:
        lin#1. open file
    file = open(filename, "r")
    #2. chekc the file avail
    if file.mode == 'r':
        #3. read
        lines = file.read()
    else:
        lines = ''
    #4. return back items
    items = lines.split
    file.close()
    return itemses = ''
    #4. return back items
    items = lines.split
    file.close()
    return items

def remove_all_non_veg(non_veg_filename, list_of_items_to_check:
    # Remove all non veg
    #1. open file
    file = open(filename, "r")
    #2. chekc the file avail
    if file.mode == 'r':
        #3. read
        lines = file.read()
    else:
        lines = ''
    #4. return back items
    items = lines.split
    file.close()
    return items
    


def save_the_list()
    # SAVE


#Mainline
#get all items
list_of_items = get_all_the_items(name_of_file)
remove_all_non_veg()
save_the_list()
#########################################
